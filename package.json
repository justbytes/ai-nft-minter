{
  "name": "ai-nft-minter",
  "version": "1.0.0",
  "type": "module",
  "description": "AI NFT Minter",
  "main": "hardhat.config.js",
  "scripts": {
    "install-all": "(npm install) && (cd blockchain && npm install) && (cd client && npm install) && (cd server && npm install)",
    "start-server": "cd server && npm run start",
    "start": "npm run install-all && npm run build && npm run start-server",
    "build": "cd client && npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  
  "devDependencies": {
    "dotenv": "^16.3.1"
  }
}
